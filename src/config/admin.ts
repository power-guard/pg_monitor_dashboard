export const CONFIG = {
  POLL_DATA_FETCH_INTERVAL: 2000, // Milliseconds. Only positive numbers larger than 0.
  PROC_START_ALERT_THRESHOLD: 360, //Seconds.
  LAST_UPDATE_ALERT_THRESHOLD: 10800, //Seconds.
} as const;
